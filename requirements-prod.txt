# Production: use with PostgreSQL on Railway/Render.
# Install: pip install -r requirements-prod.txt
# pg8000 is pure Python (no libpq.so); avoids "libpq.so.5" errors on Railway.
-r requirements.txt
pg8000>=1.30.0
